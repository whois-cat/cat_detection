# GENERATED by tools/configure.py from cameras.yaml — do not edit.
services:
  detector-camera:
    build: ./detector
    container_name: detector-camera
    restart: unless-stopped
    depends_on: [mediamtx]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      CAMERA_ID: "camera"
      RTSP_SOURCE: "rtsp://host.docker.internal:8554/camera"
      DETECTOR_TYPE: "yolo"
      BLOB_BRIGHT_THRESHOLD: 245
      BLOB_MIN_AREA: 5000
      YOLO_WEIGHTS: "/opt/models/yolov8n_int8_openvino_model/"
      YOLO_CONF: 0.25
      DETECTOR_UNKNOWN_CONF: 0.5
      CLASSIFIER_PAD_FRAC: 0.15
      DETECT_ROI: "0,0,1,1"
      IGNORE_REGIONS: "[]"
      ACTION_POLYGON: "0,0,1,1"
      FRAME_ROTATE_DEG: 90
      EVENTS_DB: "/data/events/events.db"
      WS_PORT: "8091"
      CONTROL_PORT: "8092"
      ARTIFICIAL_DELAY_MS: 0
    volumes:
      - ./data/events:/data/events
