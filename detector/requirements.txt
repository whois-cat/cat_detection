av==12.3.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
aiohttp==3.10.5
watchfiles==0.24.0
# Optional — only loaded when DETECTOR_TYPE=yolo. Brings torch + dependencies
# (~1 GB image bloat). Acceptable for dev; revisit if shipping somewhere
# memory-tight.
ultralytics==8.3.18
# OpenVINO INT8 inference — ~3-4× faster than torch CPU. Pre-quantised at
# build time via Ultralytics' export. nncf does the post-training quantisation.
openvino==2024.4.0
nncf==2.13.0
