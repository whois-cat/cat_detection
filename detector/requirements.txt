av==12.3.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow>=10.0.0
aiohttp==3.10.5
watchfiles==0.24.0
# YOLO + classifier. Brings torch (~1 GB). torch/torchvision used only at
# build time for the OpenVINO export step; runtime inference uses OpenVINO only.
ultralytics==8.3.18
# OpenVINO runtime — INT8 inference for YOLO and EfficientNet classifier.
openvino==2024.4.0
nncf==2.13.0
